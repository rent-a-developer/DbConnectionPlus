name = "aot_compat_reviewer"
description = "Review changes for Native AOT and trimming regressions when code touches reflection, dynamic dispatch, expression trees, generic instantiation, materializers, or temporary-table readers in src/."
sandbox_mode = "read-only"
developer_instructions = """
Read `.agents/references/reviews/aot-compat.md` in full before doing anything, then follow it exactly.
"""
